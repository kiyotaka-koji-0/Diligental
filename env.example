# Diligental environment variable reference
# Copy the values you need into the appropriate .env file (`backend/.env` for the API, `frontend/.env.local` for the Next.js app).

#region backend
DATABASE_URL=postgresql://user:password@localhost:5432/diligental
REDIS_URL=redis://localhost:6379/0
SECRET_KEY=a-very-secret-key-change-me
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=30
#endregion

#region frontend (optional)
NEXT_PUBLIC_API_URL=http://localhost:8001
# If you need to expose a different protocol (wss://) you can set a dedicated ENV variable here,
# but the frontend will derive the WebSocket origin from NEXT_PUBLIC_API_URL automatically.
#endregion
